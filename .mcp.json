{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Fetches up-to-date library docs (Vue 3, Element Plus, Vite) to prevent API hallucinations"
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp"],
      "description": "Browser automation for testing interview question pages"
    }
  }
}
